import { Preference } from "../../models/Preference"

const CreatePreferenceService = async (preference: Preference): Promise<string> => {
   return 'https://'
};

export default CreatePreferenceService;